import { StudentAppTitleStrategy } from './student-app-title-strategy';

describe('StudentAppTitleStrategy', () => {
  it('should create an instance', () => {
    expect(new StudentAppTitleStrategy()).toBeTruthy();
  });
});
